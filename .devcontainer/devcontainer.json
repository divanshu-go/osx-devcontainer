{
    "name": "OSX Dev Container",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "/usr/bin/tini -s /run/entrypoint.sh",
    "runArgs": [
        "--device=/dev/kvm",
        "--cap-add",
        "NET_ADMIN",
        "--stop-timeout",
        "120"
    ]
}